// Re-exports useThemeContext under the friendlier name useTheme so callers
// don't need to know about the internal context module.
export { useThemeContext as useTheme } from '../context/ThemeContext';
